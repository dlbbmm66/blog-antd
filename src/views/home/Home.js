import React, { Component } from 'react'

// 引入路由：

export default class Home extends Component {
    render() {
        return (
            <div>
                home 组件
            </div>
        )
    }
}
